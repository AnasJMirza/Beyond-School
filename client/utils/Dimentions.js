import { Dimensions } from "react-native";

export const windoeWidth = Dimensions.get("window").width;
export const windowHeight = Dimensions.get("window").height;

// Getting mobile screen height and widht from react native to use further in out application